import React from "react";
import ToolbarComponent from "./toolbar";

const Navbar = () => {
    return (
        <div className="App">
            <ToolbarComponent />
        </div>
    );
};

export default Navbar;
